<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Wake lock for hands-free recording (PARTIAL_WAKE_LOCK only) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Phone state for auto-stopping recording during calls (TSD 5.2) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".GroqVoiceApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroqVoiceKeyboard"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ===== IME Service ===== -->
        <service
            android:name=".ime.VoiceInputMethodService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true"
            android:theme="@style/Theme.GroqVoiceKeyboard">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- ===== Onboarding Activity (launcher entry point) ===== -->
        <activity
            android:name=".ui.onboarding.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.GroqVoiceKeyboard.Onboarding"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== Settings Activity ===== -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.GroqVoiceKeyboard.Settings"
            android:parentActivityName=".ui.onboarding.WelcomeActivity" />

    </application>

</manifest>
